#ifndef ROBOCALC_DATATYPES_H_
#define ROBOCALC_DATATYPES_H_

#include <string>
#include <set>
#include <tuple>
 
enum Status {
	noGas, gasD
};
enum Status {
	noGas, gasD
};
enum Angle {
	Left, Right, Back, Front
};
enum Loc {
	left, right, front
};
enum Angle {
	Left, Right, Back, Front
};
enum Status {
	noGas, gasD
};
enum Loc {
	left, right, front
};
enum Loc {
	left, right, front
};
enum Turn {
	Reverse, Port90, NoTurn, Starboard90, Starboard180
};
enum Status {
	noGas, gasD
};
enum Angle {
	Left, Right, Back, Front
};
enum Angle {
	Left, Right, Back, Front
};
enum Angle {
	Left, Right, Back, Front
};
enum Status {
	noGas, gasD
};

#endif

module OP_shortRandomWalk
exports
	Timed(OneStep) {
	D__(id__) = let
	AUX(N) = if (N == 0) then SKIP else
		  SKIP
	      	      	within AUX(shortRandomWalk_BOUND)
	
	O__(id__) = D__(id__)
	}
endmodule

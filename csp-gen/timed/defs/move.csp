module OP_move
exports
	Timed(OneStep) {
	D__(id__,
				lv,
				a) = let
	AUX(N) = if (N == 0) then SKIP else
		  SKIP
	      	      	within AUX(move_BOUND)
	
	O__(id__,
				lv,
				a) = D__(id__,
				lv,
				a)
	}
endmodule
